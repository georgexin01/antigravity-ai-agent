# Active Missions

This folder is currently empty — no missions in progress.

Completed missions live in `../../4_archive/missions/`.
Abandoned/dead plans live in `../../4_archive/_relics/missions/`.

New missions should be created here as `task_{name}.md` + `implementation_plan_{name}.md`, and upon completion paired with `walkthrough_{name}.md` before archiving as a triplet.

---
Last cleared: 2026-04-24
