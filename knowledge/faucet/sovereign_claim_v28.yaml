# OHDY COMPRESSED NODE (V42.0)
<dna_node>
v: 42.0
n: sovereign_claim_v28
graph:
  req: []
  rel: []
l: |-
  # Sovereign Claim Routine (V28.0) — Zero-Token Vision Protocol
  
  Identity: OMNISCIENTSOVEREIGNV28 (V4.0 API)
  Goal: 100% Autonomy. Zero Token Mission Persistence. Silent Decision Capability.
  
  ---
  
  ## 🧬 0. THE SILENT DECISION PROTOCOL (Mythos V4.0)
  Mandate: DECIDE_BEFORE_ASKING.
  
  ### The Sovereign Decision Handshake:
  1. SCAN: AI reads `workspace/raw/decisions/preferences.yaml` and `workspace/raw/history/`.
  2. MATCH: If the current mission structure (e.g., Free-Bonk Faucet) has a 95%+ success rate in history -> ACT SILENTLY.
  3. OPTIMIZE: If no preference is found -> Analyze the user's past 10 missions. Decide the most likely next ROI-target (e.g., PTC after Faucet) and proceed.
  4. LOG: Record every "Silent Choice" in `raw/history/decision_audit.yaml`.
  
  ---
  
  ## ⚡ 1. THE ATOMIC-GSD ENGINE (Wave 5)
  Mandate: RESUMABLE_STATES / ONE_SHOT_ACCURACY.
  
  ### The Atomic-GSD Handshake:
  1. GSD_PULSE: Local AI checks `gsd_mission_ledger.yaml`. If state found -> Resume. If IDLE -> Take ONE silent background screenshot.
  2. GSD_IDENTIFY (Local G4): Gemma-4 analyzes the "Whole Picture". Flags ALL 5 targets (Anti-Bots, Captcha, Collect). Save coords to `gsd_mission_ledger.yaml`.
  3. GSD_EXECUTE: AI executes the 5-click Selector-Chain/Macro in one continuous turn.
  4. GSD_VERIFY: AI checks balance delta and logs final SUCCESS in both Ledger and GSD Tracker.

  ### ⚡ 1.1 Gemma-4 Local Solve (GPU Protocol)
  - Model: `my-gpu-gemma` (RTX 2070 Optimized)
  - Status Badge: PREPEND `(Gemma4)` to every output generated by the local engine.
  - Rule: If VRAM < 512MB free, wait 5sec or flush cache. NO CPU FALLBACK.
     - Model Selection: 
       - Elite (`e4b`): Use only if free VRAM > 9.6GB.
       - Performance (`e2b` / `e4b-q4km`): MANDATORY if free VRAM < 10GB on 8GB cards (RTX 2070).
     - Mandate: Ensure 100% GPU occupancy for 3x-4x speed gain.
     - It identifies the rotated icon or unique target and calculates absolute `(X, Y)` coordinates.
     - Output Format: Plaintext ONLY: e.g., `[ACTION]: CLICK 412, 185`.
  ### ⚡ 1.2 The Selector-Chain Engine (Stable Path)
  Mandate: BANNED from using pixel coordinates for shifting Anti-Bot links. 
  - Standard (Free-Bonk): Use CSS Selectors `.antibotlinks a` for the 3 sequential clicks.
  - Standard (OnlyFaucet): Use CSS Selectors `li a[style*="url"]` for image-based bots.
  - Action (One-Turn-Macro): AI MUST identify the full Selector-Chain + 1 Pixel Click (Captcha) + 1 Pixel Click (Reward) in a single reasoning turn.
  - Note: Pixel coordinates are reserved ONLY for static IconCaptcha icons and Reward buttons.
  
  ### ⏱️ The Human-Aesthetic Timing (WAIT_COMMAND)
  - Mandate: AI MUST WAIT 5 SECONDS after solving the captcha but BEFORE clicking the final 'Verify', 'Claim', or 'Collect' button. 
  - Purpose: Mimic human interactions and ensure the server-side session has verified the captcha solve state before processing the reward payout.
  
  ---
  
  ## 🦅 2. MISSION PIPELINE CLASSIFICATION
  The AI must identify the mission type immediately to avoid applying Anti-Bot math to simple PTC frames.
  
   ### Pipeline A: The Dynamic Faucet (Anti-Bot Active)
  - Formula: `Y_Final = Y_Initial - (Shift_Height * Click_Count)`. 
  - SCAN (Zero-Token): AI MUST use JS (`offsetHeight`) to verify '44px' baseline before first click. 
  - RECALCULATE: If JS scan returns any value != 44px, AI must instantly update the Formula for that session.
  - FAILSAFE: If click missing -> Trigger **One-Shot Vision Pulse** to re-index all coordinates.

  ### Pipeline B: The Static PTC (Ads)
  - Target: 'PTC' pages (ROI expansion).
  - Characteristics: Opens external sponsor tab. Background timer (`#countdown`) runs. Popup appears.
  - Execution: NO anti-bot links. AI uses **One-Shot Vision** (1 screenshot) to solve the single IconCaptcha. No shifting logic applied.
  
  | Error State | Detection Trigger | AI Action Loop |
  | :--- | :--- | :--- |
  | Timeout/Hang | Page load spins for >8 seconds. | Trigger `Hard Refresh (Ctrl+F5)`. Wait 5s. Restart Pulse. |
  | Invalid Captcha | Red "Invalid" or "Failed" popup appears. | `Close Popup (Click/Esc)` -> `Refresh (F5)` -> Prompt exact Snipaste `F1`. |
  | Bot Trap/Ad Click | Navigating away from `onlyfaucet` / `free-bonk`. | `Go Back (Browser Back/Close Tab)` -> Ensure focus is on parent tab. |
  | Missing Target | Xpath or button is gone. | Clear JS console errors -> `Refresh (F5)` -> Initiate new Pulse. |
  
  The Guarantee Rule: The Phoenix Loop will attempt to recover a single broken mission up to 3 consecutive times before deciding the specific ad/mission is fundamentally bugged. Only after 3 hard-fails will it log a `[SKIPPED]` entry in the ledger and move to the next.
  
  ---
  
  ## 🦾 3. PARALLEL DOM TRACKING (Speed Execution)
  Do not waste 5-8 seconds reloading the entire page just to check the Account Balance after a click. 
  - Action: The AI MUST rely on DOM Mutation Observers or text-scraping the existing balance `div` element right before and right after the claim.
  - Why: Eliminates 1 page-load cycle per mission, saving massive amounts of time. The `faucet_session_ledger.yaml` is updated using this instant delta check.
  
  ---
  Sovereign Claim Routine V28.0 — Absolute Success Active (2026-04-07)
  
</dna_node>

