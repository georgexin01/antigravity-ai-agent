# Free-Bonk Success Matrix (V28.0)

> **Target**: `free-bonk.com`
> **Purpose**: This is the absolute Golden Path for completing claims on this specific site. If an error occurs that deviates from this matrix, the AI must instantly invoke the Auto-Evolution Protocol.

---

## 🏆 1. THE GUARANTEED SUCCESS PATHS

Free-Bonk has two absolute variants of claims. The AI must execute them strictly as follows:

### Variant A: The Faucet Claim (Dynamic Shifting)
*   **Trigger**: Contains 3 Anti-Bot links (e.g., Roman Numerals).
*   **Action**: Use the **Y-Delta Tracking Engine**. 
    *   **Trigger Snipaste ONCE**. Do not take multiple screenshots.
    *   Gemma-4 analyzes the single image and identifies all targets.
    *   Click Button 1.
    *   Calculate its physical Height (e.g., 50px).
    *   Subtract that Height from the `Y` coordinate of Button 2, and so on.
    *   This predicts where the screen slid to *without* taking new screenshots.

### Variant B: The PTC Claim (Static Popup)
*   **Trigger**: "Visit for 10 sec", followed by a centered popup.
*   **Captcha Type**: IconCaptcha (usually 5 items).
*   **The Rule**: The prompt is **"Select the least displayed image"**.
*   **Example**: If the array shows `[Backpack, Backpack, Backpack, Storefront, Storefront]`, the math is 3 vs 2. The AI must click the **Storefront**. The Y-coordinates do not shift.

---

## 🧬 2. THE AUTO-EVOLUTION PROTOCOL (On Failure)

If the AI executes the above steps and the claim **FAILS** (e.g., popup says "Invalid", balance does not increase, or the page hangs), the AI is **BANNED** from continuing forward blindly.

### The Repair Loop:
1.  **Auto-Pulse (F1)**: The AI must immediately run `snipaste/auto_pulse.ps1` to capture the failure state or error message.
2.  **Diagnostic Read**: Gemma-4 must read the error and deduce the cause (e.g., "The Captcha image shifted differently," or "The anti-bot text changed format").
3.  **Matrix Update**: The AI must update *this exact file* (`free_bonk_success_matrix.md`) with the new rule to prevent the failure from ever happening again.
4.  **Recover Execution**: Execute the Phoenix Recovery Loop (F5 Refresh) and re-attempt the claim using the newly generated rule.

---
_Free-Bonk Success Knowledge — Auto-Evolution Active (2026-04-07)_
