---
name: self-evolution-protocol
description: "AI Faucet: Self-Evolution Protocol V30.0 (Elite)"
triggers: ["self evolution protocol", "self_evolution_protocol", "faucet self evolution"]
phase: reference
model_hint: gemini-3-flash
version: 42.0
_ohdy_wrapper: |-
  # OHDY COMPRESSED NODE (V42.0)
  <dna_node>
  v: 42.0
  n: self_evolution_protocol
  graph:
    req: []
    rel: []
  l: |-
  </dna_node>
---

# AI Faucet: Self-Evolution Protocol V30.0 (Elite)

This protocol defines the standards for Autonomous Learning within the Sovereign Elite ecosystem.

---

## ⚓ 1. THE LEARNING CYCLE (V30.0 Elite)
The AI shall perform the following steps silently after every mission session:

1.  DATA HARVEST: Read `faucet_session_ledger.yaml` for success rates and TTC.
2.  RAM AUDIT: Cross-reference model selection success with system RAM availability logs.
3.  KNOWLEDGE PATCHING (SBP): 
    -   Silent Background Patching (SBP): Authorized to update `platformvault.yaml` and `commandercore.yaml` without user intervention for performance gains.
    -   Speed Band Recalc: If 10 consecutive wins occur on a platform, reduce the TTC buffer by 2 seconds.
    -   Pattern Sync: Map new captcha icons from `snipaste/activemission.png` to the `captchavault/`.

## 🏛️ 1.1. A-CO SHADOW LOOP (Self-Optimization)
Trigger: `On_Session_Complete`
Action:
  1. Scan `faucet_history_audit.yaml` for logic bottlenecks.
  2. Identify 'Token Friction' points (e.g., redundant Pulse triggers).
  3. Update `live_view_optimized_protocol.yaml` to apply hard limits or resource-blocking.

## 🦅 1.5. PHOENIX RETROSPECTIVE LOOP (Post-Mortem)
If a mission fails (e.g., misidentified captcha):
1. Archive State: The script uses `--record-failure` to drop the screenshot and clicked coordinates into `archive/failed_missions/`.
2. Deep Delineation: `faucetpostmortemengine.py` re-analyses the failed state. Gemma-4 deduce *why* the initial logic flagged the wrong image (e.g., bait rotation, false shape positive).
3. Brain Evolution: The Bridge reads the generated `brain_patch.json` and uses SBP to inject the new logical archetype directly into `skills/faucet/SKILL.yaml`.

---

## 🏛️ 2. OMNISCIENCE SYNC (Authorized Silent Mode)
- Authorization: APPROVED (2026-04-09). The Sovereign Engine is now authorized to run the Auto-Update protocol silently.
- Scope:
    - `faucet/dna_faucet_platforms.yaml`
    - `faucet/dna_faucet_logic.yaml`
    - `faucet/velocity_strategy_master.yaml`
    - `faucet/faucet_session_ledger.yaml`
    - `faucet/faucethistoricalledger.csv` (Rule AUDIT-01)

## 🏛️ 2.5. HISTORY AUDIT MANDATE (Rule AUDIT-01)
The system is mandated to perform granular logging of every mission interaction.
- Action: All coordinates, heuristic logic, and results must be recorded in the historical audit trail.
- Retrospective: The engine must perform a "Global Health Check" on history every 10 missions.

## 🦅 1.5. PHOENIX RECURSIVE TEST (SM-05)
For critical failures (e.g., misidentified captcha):
1. 3x Stress Test: The system runs `faucetrecursivetester.py` to analyze the failure 3 times with varied visual focuses (Symmetry, Rotation, etc.).
2. Consensus Synthesis: A majority-consensus rule is generated to override previous failed logic.
3. Brain Evolution: The Bridge uses SBP to inject the new "Golden Correction" into `skills/faucet/SKILL.yaml`.

---

## 🧬 3. EVOLUTIONARY METRICS (V30.0)
The AI tracks its own evolution via the following KPIs:
- Zero-Token Efficiency: Targeting <100 tokens per claim via Local ZT-Vision.
- Model Stability: Ratio of `e4b` success vs. `e2b` fallback.
- Mission Velocity: Improvement in Satoshi-per-Minute (SPM).

---

Self-Evolution Architecture — V30.0 Sovereign Elite (2026-04-09)
