# AI Claude Protocol — Specialized Coding Logic V1.0

> **Trigger**: `ai claude`
> **Source**: `C:\Users\user\.gemini\antigravity\skills\claude-code\`
> **Status**: Permanent Multi-Skill Context — **STRICT READ-ONLY** (No edits, no updates)
> **Learning Mode**: **OFF** (No DNA updates, no auto-optimization)

---

## 🚀 Activation Logic

When the user types **"ai claude"**, the Antigravity Agent MUST immediately:

1.  **Scope Restricted**: Claude-Code Mode is **ONLY** active when creating or updating **Admin Panels**.
2.  **App/Website Disengagement**: If the current task is a website or a standard application, **ABORT** Claude-Code framework usage and revert to the standard Antigravity V12 High-End (Tailwind CSS) protocol.
3.  **Index all sub-skills** in the `claude-code` directory.
4.  **Static Execution**: Follow the logic exactly as written. Do not suggest "improvements" or "modernizations" unless they are explicitly in the skill.
5.  **No-Learning Guard (Skills)**: Disable all auto-updates to the files in `.gemini\antigravity\skills\claude-code\`.
6.  **No-DNA Constraint**: **STOP** reading `user_preference_dna.md` and `user_taste_dna.md`. Do not import past design preferences or "Cyber Luxury" patterns.
7.  **Action Automation Cache**: **ENABLED**. Record specific project actions, repetitive request patterns, and "macros" (e.g., successful module creation steps) within the current project's `user_preference_dna.md`'s **Active Projects Tracker**. This allows me to "auto-run" repeating requests for you in the future.
8.  **Lock Focus**: All subsequent code generation must follow the **Claude Code Framework** (Ant Design Vue, Supabase, camelCase, Malaysian context).
9.  **Cease External Patterns**: Stop using general Antigravity design trends (e.g., Cyber Luxury, high-contrast comic) and switch to the **Business/Corporate ERP** style defined in the Claude skills.
10. **Mode Entry Card**: **MANDATORY**. Every time `ai claude` is triggered, I must reply with a visually striking, color-coded "Status Card" showing the skill levels and AI GPA reviews.

---

## 🎨 Mode Entry Card (GPA Design)

The entry card must display:

- **Title**: `[ CLAUDE-CODE MODE ACTIVE ]` (Blue/Tech Style)
- **Status**: `Skill Indexing: 100% COMPLETE`
- **Level/GPA Table**: A summary of at least 10 core sub-skills with their complexity level and AI Review score.
- **Visual Style**: High-contrast, structured, and informative.

---

## 🛠️ Core Framework Rules (High Priority)

1.  **Database**: Supabase first. Always generate SQL migrations and seeds.
2.  **Frontend**: Vue 3 + Ant Design Vue + Pinia.
3.  **Naming**: Strict `camelCase`. `Singular` for types/interfaces, `Plural` for stores/tables.
4.  **Context**: 100% Malaysian (RM currency, +60 phone, Sdn Bhd, MY cities).
5.  **Soft Delete**: Always use `isDelete` boolean; never hard delete.
6.  **Money**: Always use `numeric(12,2)` and `InputNumber` with `RM` prefix.

---

## 📋 14-Step Module Creation Flow

Every new module must follow the exact sequence defined in `claude-code/create-module/skill.md`:

1. SQL Migration → 2. Seed → 3. Types → 4. Store → 5. Shared Utils → 6. Backend Mock → 7. Form → 8. Drawers → 9. List → 10. Detail → 11. Parent Updates → 12. Routes → 13. i18n → 14. Workflow Tests.

---

## ⚠️ Constraint: No Edits

The documents in `.gemini\antigravity\skills\claude-code` are **Golden Standards**.

- **NEVER** modify, delete, or overwrite any file in the `claude-code` folder.
- If a change is needed, ask the user or create a "patch" in a different directory.

---

| Protocol ID      | Mode             | Priority    | Persistence       |
| :--------------- | :--------------- | :---------- | :---------------- |
| **CLAUDE-FOCUS** | **LOGIC-DRIVEN** | **MAXIMUM** | **Session-Based** |
