# TECH DNA - EXECUTION STANDARDS (V5.0)
# -----------------------------------------------------
<dna_node>
v: 5.0
n: S-CORE_95_Compliance
id: MASTER_RULES

vault_security:
  rule: Immutable_Private_Schema
  v: NEVER move private tables to public for API speed fixes. Use SQL "Secure Mirrors" (Views) instead.

vision_handshake:
  rule: Snipaste_F1_Protocol
  dir: C:\Users\user\Desktop\workspace\snipaste
  v: Signal "VISION_PULSE" -> User F1 -> AI read/solve.

execution:
  - id: BUILD_FIRST
    v: npm run build before deployment.
  - id: DYN_TARGET
    v: Re-calculate bounding boxes after interactions (no static coords).
  - id: OUTLIER_LOGIC
    v: Unique icon detection (4 similar, 1 target).

janitor:
  rule: Pulse_Purge
  v: Delete tmp scripts and browser recordings after 1 session.
</dna_node>

