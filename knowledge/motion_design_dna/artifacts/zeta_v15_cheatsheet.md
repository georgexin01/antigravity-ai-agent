# ZETA V15 'Hyper-Density' DNA Cheatsheet

### 🎨 Visual Core (The Purple Void)
- **Primary**: `#a855f7` (Bioluminescent Purple)
- **Background**: `#050505` (Noir Deep Black)
- **Glass**: `blur(20px)` + `rgba(5, 5, 5, 0.4)` + `1px solid var(--glass-border)`
- **Corners**: `20px` (Components) / `0px` (Industrial Badges)

### 📏 Typography Hierarchy
- **Header/Footer Labels**: `10px / Weight 700 / Letter-Spacing 3px`
- **Main Headlines**: `clamp(50px, 5vw, 130px) / Weight 900 / Line-Height 0.9`
- **Secondary Badges**: `9px / Weight 900 / Letter-Spacing 8px` (S-CORE Standard)

### 💨 Motion DNA (Master Suite)
1.  **Hardware Video Sync**: 0.5s fade-in triggered by `heroVideo.play().then()`.
2.  **Inception Scramble**: `gsap.from('.hero-content-v3 > *')` with stagger, triggering `scrambleText()` callback.
3.  **Digital Pulse**: `logo-pulse` animation (scale 1.0 to 1.5) on status indicators.
4.  **Magnetic Protocol**: Hover offset `x * 0.3, y * 0.3` on all navigational elements.

### ⚓ Footer Arch (Johor Bahru Footprint)
- 4-Column Layout: `Corporate` | `Navigation` | `Products` | `Connectivity`.
- Must include: `ISO_DIGITAL_CERTIFIED` and `POWERED BY ANTIGRAVITY ENGINE V16.2`.
